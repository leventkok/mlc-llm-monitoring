# Copy to .env.docker in deployments/ and adjust if needed.
POSTGRES_USER=app
POSTGRES_PASSWORD=app
POSTGRES_DB=app_review

JWT_SECRET=dev-only-secret-change-in-production-local
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

GATEWAY_HOST_BIND=127.0.0.1
GATEWAY_PORT=8080

PROMETHEUS_HOST_BIND=127.0.0.1
PROMETHEUS_PORT=9090

GRAFANA_HOST_BIND=127.0.0.1
GRAFANA_PORT=3001
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin
